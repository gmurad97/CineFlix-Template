import "./Section.css";

const Section = ({ title, children }) => {
    return (
        <section className="section">
            <h1>Section Title</h1>
        </section>
    );
}

export default Section;